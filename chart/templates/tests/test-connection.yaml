apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "namespace-node-affinity.fullname" . }}-test-connection"
  labels:
    {{- include "namespace-node-affinity.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "namespace-node-affinity.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
